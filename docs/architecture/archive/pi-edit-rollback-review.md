- [BLOCKER] `docs/architecture/pi-edit-fastedit-integration.md:Subprocess contract` — Spec requires `--snippet -`, `--edits -`, `--file-edits -`, but `shared/subprocess.ts:SpawnOptions` has no stdin support and `Bun.spawn` is created without writable stdin. Timeout also returns exit `124`, not throw. Proposed fix: extend `spawnCollect` with `stdin?: string | Uint8Array`, write/close stdin, pass abort signal, mark truncation, and make wrapper explicitly map `124`/spawn errors to hard failures.

- [BLOCKER] `docs/architecture/pi-edit-fastedit-integration.md:Tool surface` — `pi_edit_rename_all` can rewrite many files with `dry_run` optional/default-false and no `confirm`. Proposed fix: make dry-run default, require `confirm: true` + explicit `apply: true` for writes, show affected files first, and refuse roots outside canonical `ctx.cwd`.

- [BLOCKER] `docs/architecture/pi-edit-fastedit-integration.md:Subprocess contract` + `docs/architecture/pi-rollback-contract.md:Adapter contract` — No canonical path/cwd policy. Absolute paths, `../`, symlink escapes, and multi-edit file paths can edit or revert outside workspace. Proposed fix: resolve every `file`/`path`/`root`/`target.path` against `ctx.cwd`, compare `realpath` under canonical cwd, reject escaping symlinks unless explicit trusted config, and pass git paths after `--`.

- [MAJOR] `docs/architecture/pi-edit-fastedit-integration.md:Config` — `tldrBinary` is not enforceable through current FastEdit CLI, which invokes literal `tldr` internally. Extension preflight override would not affect `fastedit read/search/rename*`. Proposed fix: remove `tldrBinary`, document PATH requirement, or add a controlled PATH shim / upstream env var before promising override.

- [MAJOR] `docs/architecture/pi-edit-fastedit-integration.md:External prerequisites / Config / Risks` — Spec claims `backend: "llm"` and Linux default `llm`, but current FastEdit CLI source exposes `--backend` choices `mlx|vllm` and backend behavior is version-sensitive. Proposed fix: pin a minimum `fastedits` version and have doctor probe `fastedit edit --help`/`fastedit doctor` for supported backends; only expose `llm` config when verified.

- [MAJOR] `docs/architecture/pi-edit-fastedit-integration.md:Exit-code mapping` — Spec says exit `1` returns `{ isError: true }`, but `pi-extension-surface-notes.md:Error signaling rule` says thrown errors are the reliable LLM failure path. Proposed fix: define hard vs soft taxonomy: throw for invalid params, confirm missing, timeout, binary missing if tool cannot run; return text/details for empty search/no backup/no changes.

- [MAJOR] `docs/architecture/pi-edit-fastedit-integration.md:Stdout parsing` — FastEdit can exit `0` after writing while printing `Warning: merged output has parse errors` or `Partial edit applied`; spec only parses `Applied...` success lines. Proposed fix: classify `Warning:` lines separately, include `details.warning`, trigger follow-up validation guidance, and consider soft-error result after mutation.

- [MAJOR] `docs/architecture/pi-edit-fastedit-integration.md:Tool surface` — TypeBox params lack byte/item limits and runtime validation for exactly one of `after`/`replace`. Large snippets or JSON edit arrays can exhaust memory/model budget; invalid target shape reaches FastEdit late. Proposed fix: add `maxLength`, `maxItems`, max aggregate bytes, reject NUL/control bytes, and encode exactly-one with schema + runtime guard.

- [MAJOR] `docs/architecture/pi-edit-fastedit-integration.md:Config` — Project `.pi/pi-edit.json` can override `binary`/FASTEDIT env and cause arbitrary command execution on doctor/session_start in an untrusted repo. Proposed fix: treat `binary` as user-config-only by default, require absolute allowlisted path for project overrides, show config source in doctor, and only pass allowlisted env vars.

- [MAJOR] `docs/architecture/pi-edit-fastedit-integration.md:Doctor preflight` — Doctor cache is `WeakMap<ExtensionAPI, DoctorSnapshot>`, not keyed by cwd/config/binary, and session_start + first tool call can race. Proposed fix: store `doctorPromise` per `{canonicalCwd, configHash, binary}` with TTL/invalidation, memoize concurrent checks, and never let a stale global doctor gate another workspace.

- [MINOR] `docs/architecture/pi-edit-fastedit-integration.md:Telemetry` — Metrics persist only on `agent_end`, unlike `flow-system` which also persists on `session_shutdown` and dedupes snapshots. Proposed fix: persist on both hooks, validate with TypeBox on restore, store a bounded snapshot, and dedupe with a stable key.

- [MAJOR] `docs/architecture/pi-edit-fastedit-integration.md:Idempotent registration` — `resources_discover` example returns path segments as separate `skillPaths` entries: `[<dirname>, "..", "skills", "pi-edit"]`. Pi expects actual skill package dirs. Proposed fix: mirror `flow-system`: `const skillDir = join(baseDir, "..", "skills", "pi-edit"); return { skillPaths: [skillDir] }`.

- [MINOR] `docs/architecture/pi-edit-fastedit-integration.md:Effect-TS decision` — “Every tool is one-shot `spawnCollect` + text parse” ignores config load, doctor cache, metrics, and lifecycle hooks. No-Effect MVP still defensible, but rationale is too broad versus `AGENTS.md` Effect stack. Proposed fix: call it an explicit small-wrapper exception and require plain mutex/promise memoization for shared state.

- [BLOCKER] `docs/architecture/pi-rollback-contract.md:Scope / Session log persistence` — `pi_rollback_session` cannot know “every file pi modified this session”; `pi_rollback_log` records rollback attempts, not edit mutations from pi-edit or other write lanes. Proposed fix: define shared mutation ledger entries with sessionId, tool, canonical file, pre/post hash, adapter backup ref, ts; require pi-edit to append them before session rollback ships.

- [BLOCKER] `docs/architecture/pi-rollback-contract.md:Adapter: fastedit-undo` — `canHandle` says non-empty `fastedit diff <file>` means backup exists, but FastEdit prints `No backup recorded...` and `No changes detected...` as non-empty stdout. Proposed fix: parse explicit no-backup/no-change lines as false and require unified diff markers or `fastedit undo` dry probe metadata before selecting adapter.

- [MAJOR] `docs/architecture/pi-rollback-contract.md:Adapter: fastedit-undo` — FastEdit backup keys hash the raw file path string; `src/a.ts` and `/repo/src/a.ts` can address same file but different backup slots. Proposed fix: mandate one canonical path form for all pi-edit/rollback FastEdit calls, preferably realpath absolute under cwd, and record that path in mutation ledger.

- [BLOCKER] `docs/architecture/pi-rollback-contract.md:Registry / Adapter: fastedit-undo / Adapter: git-stash` — No preimage hash, mtime, or lock guards between history/dryrun/revert. `fastedit undo` or `git restore` can erase user/agent edits made after preview. Proposed fix: return preview token containing current hash + versionRef, verify before revert, serialize per-file/repo with lockfiles, and refuse when current hash changed.

- [BLOCKER] `docs/architecture/pi-rollback-contract.md:Adapter: git-stash` — “`git stash apply <ref>` with conflict detection; on conflict, abort and return without mutating tree” is unsafe: stash apply can leave partial conflict state. Proposed fix: preflight with `git stash show -p <ref> | git apply --check` or temp worktree/index; if not provably clean, refuse before mutation.

- [MAJOR] `docs/architecture/pi-rollback-contract.md:Registry / Adapter: git-stash` — `git-stash` priority and `canHandle` are not target-specific. Any stash or dirty status can make adapter available even when stash does not touch target path. Proposed fix: split `git-working-tree` from `git-stash`, filter stashes via `git stash show --name-only`, and sort versions per target path/session.

- [MAJOR] `docs/architecture/pi-rollback-contract.md:Adapter: git-stash` — Working-tree revert semantics ignore staged vs unstaged vs untracked files. `git checkout -- <path>` restores worktree from index but leaves staged changes; session revert promise remains false. Proposed fix: define modes for unstaged, staged, both, and untracked; use `git restore --worktree/--staged` intentionally and preview each bucket.

- [MAJOR] `docs/architecture/pi-rollback-contract.md:Config` — `requireConfirm?: boolean` lets config relax destructive confirmation, contradicting `Tool surface` hard-fail rule and `AGENTS.md` dangerous-op confirmation rule. Proposed fix: remove relaxation for destructive tools, or restrict to explicit operator-auth noninteractive mode; never disable for session-wide revert.

- [MAJOR] `docs/architecture/pi-rollback-contract.md:Adapter: git-stash` — In-flight git-state detection via `.git/MERGE_HEAD` etc breaks in linked worktrees/submodules where `.git` is a file. Proposed fix: use `git rev-parse --git-dir --show-toplevel`, inspect git-dir state files (`MERGE_HEAD`, `CHERRY_PICK_HEAD`, `REVERT_HEAD`, `rebase-*`, `sequencer`), and refuse submodules unless explicitly supported.

- [MAJOR] `docs/architecture/pi-rollback-contract.md:Effect-TS decision` — “Every call is a single subprocess” is false for session rollback, stash preview/apply, conflict checks, and git-state checks. No Effect may still be OK, but MVP needs explicit sequencing/locking/partial-failure policy. Proposed fix: either de-scope `pi_rollback_session apply` from MVP or specify a plain async transaction runner with per-repo locks.

- [MINOR] `docs/architecture/pi-rollback-contract.md:Session log persistence` — Appending one `pi_rollback_log` entry per attempt conflicts with “bounded to retention” unless each append stores a bounded snapshot. Proposed fix: persist `{ entries: lastN }` snapshots like flow-system queue state, validate on restore, and dedupe unchanged snapshots.

- [MINOR] `docs/architecture/pi-rollback-contract.md:Tool surface / Non-goals` — No TUI or command affordance for adapter selection despite risky destructive ops; tool-call-only forces LLM to choose from raw text. Proposed fix: keep MVP tool-only if needed, but add structured `details` for history/dryrun plus copyable next-call hints; later `/rollback` overlay can consume same details.

- [MINOR] `AGENTS.md:Stack / Extension conventions` vs `docs/architecture/pi-edit-fastedit-integration.md:Effect-TS decision` and `docs/architecture/pi-rollback-contract.md:Effect-TS decision` — Specs say “No Effect MVP” while repo context lists Effect-TS as resilience stack. Proposed fix: add one sentence in each spec: “Intentional AGENTS exception for one-shot subprocess wrappers; add Effect only when queue/retry/transaction triggers land.”
