{
  "name": "@codewithkenzo/pi-rig",
  "version": "0.1.0",
  "description": "Installer CLI for Pi Rig, a compact Pi coding agent workflow stack for execution, messaging, routing, and workflow state",
  "type": "module",
  "main": "./dist/lib.js",
  "bin": {
    "pi-rig": "./dist/cli.js"
  },
  "scripts": {
    "build": "bun build ./src/cli.ts --outfile ./dist/cli.js --target node && bun build ./src/lib.ts --outfile ./dist/lib.js --target node",
    "typecheck": "tsc --noEmit",
    "test": "bun test"
  },
  "keywords": [
    "pi",
    "pi-coding-agent",
    "pi-package",
    "installer",
    "workflow",
    "agent-tools",
    "cli"
  ],
  "devDependencies": {
    "@types/bun": "latest",
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  }
}
